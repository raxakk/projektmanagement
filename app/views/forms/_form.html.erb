<%= form_for(@form) do |f| %>
  <div class="field">
    <%= f.label :optimistisch %><br>
    <%= f.text_field :opt, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :pessimistisch %><br>
    <%= f.text_field :pes, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :realistisch %><br>
    <%= f.text_field :real, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :mitarbeiter %><br>
    <%= f.select(:worker_id) do %>
      <%= options_from_collection_for_select(@workers, :id, :name) %>
    <% end %>
  </div>
  <div class="field">
    <%= f.hidden_field :round_id %>
  </div>
  <div class="actions">
    <%= f.submit 'Schätzung abgeben', class: 'btn btn-success' %>
  </div>
<% end %>
