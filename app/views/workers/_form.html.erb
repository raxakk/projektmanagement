<%= form_for(@worker) do |f| %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit 'Speichern', class: 'btn btn-success' %>
  </div>
<% end %>
