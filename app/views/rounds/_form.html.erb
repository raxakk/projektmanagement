<%= form_for(@round) do |f| %>

  <div class="field">
    <%= f.label :expertenanzahl %><br>
    <%= f.number_field :member_number, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit 'Runde erstellen', class: 'btn btn-success' %>
  </div>
<% end %>
